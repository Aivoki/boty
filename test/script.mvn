chrome.open('https://docs.marvin.run/pt_br/')
chrome.wait_load($browser)
console.warning('Marvin is working')
prompt.alert('If you are seen this message, everything is good!!!!')
# chrome.close($browser)
